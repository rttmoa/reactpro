function splitPhone(str) {
  if(str.length !== 11) return str

  var arr = str.split('')

  // 数组中插入 -
  arr.splice(3, 0, '-')
  arr.splice(8, 0, '-')
  
  // 数组转字符串并返回
  return arr.join('')
}

module.exports = {
  splitPhone: splitPhone
}